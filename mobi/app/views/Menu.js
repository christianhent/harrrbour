App.views.Menu = Ext.extend(Ext.Panel, {    dockedItems: [{        xtype: 'toolbar',        title: 'Harbour',		items: [			{xtype: 'spacer'},			{				xtype: 'button',				text: 'About',				handler: function() {					Ext.Msg.alert('harrrbour 0.1', 'Nooku meets Sencha', Ext.emptyFn);				}			}		]}],    items: [{        xtype: 'list',        store: App.stores.Menu,        itemTpl : '{title}',        onItemDisclosure: function (record) {            Ext.dispatch({                controller: App.controllers.Default,                action: 'browseItems',                id: record.getId()            });        },        grouped: false,        scroll: 'vertical',        fullscreen: true    }],    initComponent: function() {        App.views.Menu.superclass.initComponent.apply(this, arguments);            }});